<% @user ||= current_user %>
    <div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat">
      <div class="text-muted"><%= @user.followings.count %>   
      </strong>フォロー</div>
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat">
       <div class="text-muted"><%= @user.followers.count %>
    </strong>
    フォロワー</div>
  </a>
</div>
    
  